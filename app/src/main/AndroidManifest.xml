<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Eruplan">

        <!-- StartupActivity è il Launcher: deve essere exported="true" -->
        <activity
            android:name=".StartupBoundary"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Le altre activity sono interne: exported="false" per sicurezza -->
        <activity
            android:name=".LoginBoundary"
            android:exported="false">
        </activity>

        <activity
            android:name=".GestioneNucleoBoundary"
            android:exported="true">
        </activity>

        <activity
            android:name=".SignupBoundary"
            android:exported="true">
        </activity>

        <activity
            android:name=".AggiungiMembroActivity"
            android:exported="true">
        </activity>

        <activity
            android:name=".CreaNucleoBoundary"
            android:exported="false">
        </activity>

        <activity
            android:name=".CosaVuoiFareActivity"
            android:exported="false">
        </activity>

        <activity
            android:name=".VisualizzaNucleoBoundary"
            android:exported="false"/>

        <service
            android:name=".EruplanMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

    </application>

</manifest>